class MineSweeper:
    def __init__(self, field):
        self.field = field
    
    def solve(self):
        for i,v in enumerate(self.field)
            if v == '\"\"\"\"\"\"\"\"'
        return self.field if self.field == "*" else "0"